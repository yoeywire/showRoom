#include "TimingEngine.h"


//TimingEngine::TimingEngine(RgbEngine* engine) {
	//rgbEngine = engine;

	//std::thread timingTrigger(&TimingEngine::timeInterupt, this);
	//timingTrigger.detach();
//}


//void TimingEngine::timeInterupt() {
//	std::this_thread::sleep_for(std::chrono::microseconds(INTERVAL));
//	wxLogDebug("<TimingEngine> frame");
//	rgbEngine->update();
//}